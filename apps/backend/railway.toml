[build]
builder = "RAILPACK"

[deploy]
startCommand = "node src/index.js"
preDeployCommand = "npx prisma generate && npx prisma db push --accept-data-loss && node prisma/seed-prod.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
